# SparkVSR ComfyUI Custom Node Requirements

# ── Core diffusion stack ──────────────────────────────────────────────────────
diffusers>=0.32.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0

# ── PyTorch (install separately from pytorch.org for your CUDA version) ───────
torch>=2.0.0
torchvision

# ── Video I/O ─────────────────────────────────────────────────────────────────
imageio[ffmpeg]>=2.34.0
imageio-ffmpeg

# ── Utilities ─────────────────────────────────────────────────────────────────
Pillow>=9.0.0
tqdm

# ── PiSA-SR inline mode (pisa_ref) ───────────────────────────────────────────
peft>=0.9.0
einops>=0.6.0

# ── Optional: Nano-Banana Pro API reference mode ──────────────────────────────
# Uncomment to enable nano-banana-pro-ref mode:
# fal-client>=0.5.0
# requests>=2.28.0
